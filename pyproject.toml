[project]
name = "klimper"
version = "0.1.0"
description = "Generate Anki flashcards with music trivia from your Spotify playlists"
requires-python = ">=3.12"
dependencies = [
    "genanki>=0.13.1",
    "openai>=2.21.0",
    "python-dotenv>=1.2.1",
    "requests>=2.32.5",
]
